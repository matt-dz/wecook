# =============================================================================
# WeCook Database Environment Variables
# =============================================================================
#
# This file contains environment variables for the PostgreSQL database container.
# Copy this file to .env.database and update with your values.
#
# Important: POSTGRES_PASSWORD must match DATABASE_PASSWORD in .env.backend
#
# =============================================================================

# PostgreSQL database password (required)
# Generate a secure password: openssl rand -base64 32
# This must match DATABASE_PASSWORD in .env.backend
POSTGRES_PASSWORD=secure-password

# PostgreSQL database username (required)
# This must match DATABASE_USER in .env.backend
POSTGRES_USER=wecook

# PostgreSQL database name (required)
# This must match DATABASE in .env.backend
POSTGRES_DB=wecook

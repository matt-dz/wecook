# =============================================================================
# WeCook Frontend Environment Variables
# =============================================================================
#
# This file contains environment variables for the SvelteKit frontend.
# Copy this file to .env.frontend and update with your values.
#
# =============================================================================

# =============================================================================
# Backend Configuration
# =============================================================================

# Internal backend API URL for server-side requests (required)
# This is used for server-side API calls within the Docker network
# In Docker Compose, use the backend service name and port
# Example: http://wecook-backend:8080
INTERNAL_BACKEND_URL=http://wecook-backend:8080

# =============================================================================
# Development Settings
# =============================================================================
# These settings are only needed for development with Docker on certain filesystems

# Enable polling for file watching in development
# Set to true when running in Docker with mounted volumes
# This helps with hot-reload functionality on some systems
CHOKIDAR_USEPOLLING=true

# Enable polling for webpack watching in development
# Set to true when running in Docker with mounted volumes
WATCHPACK_POLLING=true

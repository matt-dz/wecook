SERVER_PORT=8080

DATABASE_USER=wecook
DATABASE_PASSWORD=password
DATABASE_HOST=database
DATABASE_PORT=5432
DATABASE=wecook

APP_SECRET=change-me
ENV=DEV
HOST_ORIGIN=http://localhost:8080

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me

FILESERVER_VOLUME=/data/files
FILESERVER_URL_PREFIX=/files

# Email Configuration
# SMTP server host (e.g., smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=smtp.example.com
# SMTP server port (587 for STARTTLS, 465 for implicit TLS, 25 for unencrypted)
# TLS is automatically enabled for ports 587 and 465
SMTP_PORT=587
# SMTP authentication username
SMTP_USERNAME=user@example.com
# SMTP authentication password
SMTP_PASSWORD=your-smtp-password
# From address for outgoing emails
SMTP_FROM=noreply@example.com

services:
  backend:
    build:
      context: ./backend/
      dockerfile: Dockerfile.dev
    container_name: wecook_backend
    hostname: wecook-backend
    restart: unless-stopped
    env_file: .env.backend
    depends_on:
      database:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/api/ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - ./backend/:/app
      - go-modules:/go/pkg/mod
      - dev-files-data:/data/files
      - dev-backend-data:/data
    #  - ./wecook.yaml:/data/wecook.yaml:ro
    stdin_open: true
    tty: true

  web:
    container_name: wecook_frontend
    hostname: wecook-frontend
    restart: unless-stopped
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend:/app
      - web-node-modules:/app/node_modules
    env_file: .env.frontend

  database:
    image: postgres:18
    container_name: wecook_database
    hostname: wecook-database
    env_file: .env.database
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 10s
      retries: 5
    volumes:
      - dev-db-data:/var/lib/postgresql

  nginx:
    image: nginx:alpine
    container_name: wecook_nginx
    hostname: wecook-nginx
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
      web:
        condition: service_started
      swagger-ui:
        condition: service_started
    ports:
      - "8080:80"
    volumes:
      - dev-files-data:/data/files:ro
      - ./fileserver.conf:/etc/nginx/conf.d/default.conf:ro

  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    container_name: wecook_docs
    hostname: wecook-docs
    environment:
      BASE_URL: /docs
      URL: /api/openapi.yaml
      DEEP_LINKING: true

volumes:
  dev-db-data:
  dev-files-data:
  dev-backend-data:
  go-modules:
  web-node-modules:
